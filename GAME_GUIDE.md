# 宰执春秋 — 游戏架构与机制文档

> **开发者**：prideplayer
> **版本**：1.5.0
> **技术栈**：Electron + Vite + React + TypeScript + Zustand + Tailwind CSS + motion/react
> **类型**：政治决策模拟 / 即时策略

---

## 目录

1. [项目架构](#1-项目架构)
2. [目录结构](#2-目录结构)
3. [核心引擎](#3-核心引擎)
4. [页面路由与导航](#4-页面路由与导航)
5. [游戏机制总览](#5-游戏机制总览)
6. [指标系统](#6-指标系统)
7. [事件系统](#7-事件系统)
8. [改革系统](#8-改革系统)
9. [政策系统](#9-政策系统)
10. [内阁系统](#10-内阁系统)
11. [议会系统](#11-议会系统)
12. [总理系统与性格特质](#12-总理系统与性格特质)
13. [突击新闻发布会（Minigame）](#13-突击新闻发布会minigame)
14. [深夜官邸密室游说棋盘（Minigame）](#14-深夜官邸密室游说棋盘minigame)
15. [质询辩论 / 信件 / 照会](#15-质询辩论--信件--照会)
16. [外交系统](#16-外交系统)
17. [组阁要求追踪机制](#17-组阁要求追踪机制)
18. [成就系统](#18-成就系统)
19. [结局系统](#19-结局系统)
20. [时间与节奏配置](#20-时间与节奏配置)
21. [存档系统](#21-存档系统)
22. [新手教程](#22-新手教程)
23. [待办提示系统](#23-待办提示系统)
24. [国家代入感与节令时序](#24-国家代入感与节令时序)
25. [难度系统](#25-难度系统)
26. [玩法指南](#26-玩法指南)
27. [法律议事系统（v1.4 新增）](#27-法律议事系统v14-新增)
28. [人物谱与派系（v1.4 新增）](#28-人物谱与派系v14-新增)
29. [宏观经济模拟引擎（v1.4 新增）](#29-宏观经济模拟引擎v14-新增)
30. [总理个人生活维度（v1.4 新增）](#30-总理个人生活维度v14-新增)
31. [跨系统联动事件（v1.4 新增）](#31-跨系统联动事件v14-新增)
32. [页面改版说明（v1.4 新增）](#32-页面改版说明v14-新增)
33. [历史曲线系统（v1.5 新增）](#33-历史曲线系统v15-新增)
34. [事件链深化（v1.5 新增）](#34-事件链深化v15-新增)
35. [NPC 主动 AI（v1.5 新增）](#35-npc-主动-aiv15-新增)
36. [战争指挥面板（v1.5 新增）](#36-战争指挥面板v15-新增)
37. [页面布局调整与 Bug 修复（v1.5）](#37-页面布局调整与-bug-修复v15)

---

## 1. 项目架构

```
┌─────────────────────────────────────────────────────────┐
│                  Electron Main 进程                      │
│  (electron/main.ts)                                      │
│  - 窗口管理 / 存档 IPC / 对话框 / 图标缓存刷新           │
└───────────────┬─────────────────────────────────────────┘
                │ contextBridge (preload.ts)
                ▼
┌─────────────────────────────────────────────────────────┐
│                  React 渲染进程                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Zustand Store (gameStore.ts)                    │   │
│  │  - 完整 GameState / 30+ actions                  │   │
│  │  - sidePanelPage / actionDialog 等导航状态        │   │
│  └────────────────────┬─────────────────────────────┘   │
│                       │                                  │
│  ┌────────────────────▼─────────────────────────────┐   │
│  │  引擎层 (src/engine/)                             │   │
│  │  - metrics.ts        指标计算                     │   │
│  │  - eventEngine.ts    事件 / 时间推进              │   │
│  │  - cardEngine.ts     议会质询卡牌                 │   │
│  │  - endings.ts        结局判定                     │   │
│  │  - legacySystem.ts   历史遗产                     │   │
│  └────────────────────┬─────────────────────────────┘   │
│                       │                                  │
│  ┌────────────────────▼─────────────────────────────┐   │
│  │  数据层 (src/data/)                               │   │
│  │  - 27 个数据文件                                  │   │
│  │  - 事件/改革/政策/内阁/政党/议会/NPC/外交/成就     │   │
│  └────────────────────┬─────────────────────────────┘   │
│                       │                                  │
│  ┌────────────────────▼─────────────────────────────┐   │
│  │  UI 层 (src/pages + src/components)               │   │
│  │  - 24 页面 + 29 组件                              │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

**分层职责**：
- **数据层**：纯静态数据，无副作用，由 TypeScript 类型严格约束。
- **引擎层**：纯函数，接收 `GameState` 返回新 `GameState`，便于测试与回放。
- **Store 层**：Zustand 单一全局状态，封装所有副作用与异步操作。
- **UI 层**：响应式组件，订阅 Store 状态变化，触发 actions。

---

## 2. 目录结构

```
Prime Minister Simulator/
├── electron/
│   ├── main.ts              # Electron 主进程入口 + 存档 IPC
│   └── preload.ts           # contextBridge 暴露 window.api
├── src/
│   ├── App.tsx              # 屏幕级路由（menu/game/ending）
│   ├── main.tsx             # React 入口
│   ├── index.css            # 全局样式 + Tailwind
│   ├── components/          # 29 个 UI 组件
│   │   ├── TitleBar.tsx              # 自定义标题栏
│   │   ├── WindowControls.tsx        # 窗口控制按钮
│   │   ├── StatusBar.tsx             # 顶部状态栏（日期/任期/指标）
│   │   ├── GameNav.tsx               # 顶部主导航 + 侧栏分组定义
│   │   ├── SideRail.tsx              # 右侧图标栏（议会/民意/内阁）
│   │   ├── SidePanel.tsx             # 右侧滑出面板容器
│   │   ├── PendingTodos.tsx          # 右侧待办提示栏
│   │   ├── TimeControl.tsx           # 时间速度 + 自动存档
│   │   ├── DailyActionsPanel.tsx     # 总理日常行动
│   │   ├── NPCChatter.tsx            # 内阁闲聊
│   │   ├── EventPopup.tsx            # 事件弹窗
│   │   ├── EventBasket.tsx           # 事件收纳篮
│   │   ├── CardEventDialog.tsx       # 卡牌事件对话框
│   │   ├── HandBar.tsx               # 卡牌手牌栏
│   │   ├── GameCard.tsx              # 单张卡牌
│   │   ├── BillVotingDialog.tsx      # 法案表决（密室政治）
│   │   ├── CountdownTimer.tsx        # 倒计时事件
│   │   ├── WarEventDialog.tsx        # 入侵事件弹窗
│   │   ├── BreakingNews.tsx          # 突发新闻头条
│   │   ├── NotificationAlerts.tsx    # 未读消息提醒
│   │   ├── ActionDialog.tsx          # 行动确认对话框
│   │   ├── MetricTooltip.tsx         # 指标悬浮提示
│   │   ├── MetricEffectBadge.tsx     # 指标效果徽章
│   │   ├── SeatArc.tsx               # 议会席位弧形图
│   │   ├── StoryBeatToast.tsx        # 国家代入感叙事弹窗
│   │   ├── AchievementToast.tsx      # 成就解锁弹窗
│   │   ├── Tutorial.tsx              # 新手教程（引导式，支持手动触发）
│   │   ├── DeveloperConsole.tsx     # 开发者控制台（右上角菜单 → 二次确认进入）
│   │   └── GameMenu.tsx              # 游戏菜单（存档/百科/设置/开发者选项）
│   ├── pages/             # 24 个页面
│   │   ├── MainMenu.tsx              # 主菜单
│   │   ├── CharacterCreation.tsx     # 角色创建（含难度选择）
│   │   ├── CoalitionNegotiation.tsx  # 组阁谈判
│   │   ├── InitialCabinetSetup.tsx   # 初始内阁组建
│   │   ├── Game.tsx                  # 游戏主容器
│   │   ├── DashboardPage.tsx         # 仪表盘（每日简报）
│   │   ├── PMProfilePage.tsx         # 总理档案
│   │   ├── TaskTreePage.tsx          # 任务树
│   │   ├── PoliciesPage.tsx          # 政策树
│   │   ├── InitiativesPage.tsx       # 改革树
│   │   ├── DiplomacyPage.tsx         # 外交
│   │   ├── MilitaryPage.tsx          # 军事
│   │   ├── EconomyPage.tsx           # 经济
│   │   ├── SocietyPage.tsx           # 社会
│   │   ├── EnvironmentPage.tsx       # 环境
│   │   ├── NewsPage.tsx              # 新闻中心
│   │   ├── EncyclopediaPage.tsx      # 百科全书
│   │   ├── ParliamentPage.tsx        # 议会
│   │   ├── DebatePage.tsx            # 质询辩论
│   │   ├── LettersPage.tsx           # 信件与照会
│   │   ├── MediaPage.tsx             # 媒体中心
│   │   ├── ElectionPage.tsx          # 大选页面
│   │   ├── CabinetPage.tsx           # 内阁名单
│   │   ├── CabinetChatPage.tsx       # 内阁聊天
│   │   └── Ending.tsx                # 结算页面
│   ├── data/              # 27 个数据文件
│   ├── engine/            # 5 个引擎文件
│   ├── store/
│   │   ├── gameStore.ts   # Zustand 全局状态
│   │   └── settingsStore.ts # 设置（含教程完成标志）
│   ├── hooks/
│   │   └── useSaveGame.ts # 存档管理 hook
│   ├── types/
│   │   └── game.ts        # 所有 TypeScript 类型
│   └── lib/
│       └── utils.ts       # 工具函数
├── build/
│   └── installer.nsh      # NSIS 安装脚本（图标缓存刷新）
├── electron-builder.yml   # 打包配置（含 NSIS 安装器图标）
└── GAME_GUIDE.md          # 本文档
```

---

## 3. 核心引擎

### `src/engine/metrics.ts` — 指标计算
| 函数 | 作用 |
|---|---|
| `clamp(value)` | 钳制到 0–100 整数 |
| `applyEffects(metrics, effects, difficulty?)` | 叠加一级指标效果，按难度调整 |
| `naturalDecay(metrics)` | 每月自然衰减：国库 -1，其余向 50 回归 ±1 |
| `average(metrics)` | 六项指标算术均值 |
| `allAbove(metrics, threshold)` | 检查全部 ≥ 阈值 |
| `deriveSecondary(metrics)` | 由一级指标推导二级指标 |

### `src/engine/eventEngine.ts` — 事件与时间推进
| 函数 | 作用 |
|---|---|
| `isEventInCooldown(state, eventId)` | 检查 240 天冷却期 |
| `pickEvent(state)` | 优先事件链 → 紧急 → 入侵 → 加权随机 |
| `checkEmergency(state)` | 指标阈值触发紧急事件 |
| `checkInvasion(state)` | 复杂条件触发入侵事件 |
| `checkCountdownEvent(state)` | 倒计时事件触发检查 |
| `resolveOption(state, event, optionId)` | 应用选项效果，处理事件链、冷却、紧急记录 |
| `advanceDay(state)` | 按日推进：日期递增 + 月末结算 + 周期性事件触发 + 入侵检查（每 30 天） |
| `advanceMonth(state)` | 月度结算：自然衰减 + 改革 perTurn/完成结算 + 议会冷却递减 + 二级指标推导 |

### `src/engine/cardEngine.ts` — 议会质询卡牌
| 函数 | 作用 |
|---|---|
| `shouldTriggerPmqs(state)` | 检查是否到 60 天质询周期 |
| `calcInitialVotes(parties)` | 法案表决初始票数计算 |

### `src/engine/endings.ts` — 结局判定
| 函数 | 作用 |
|---|---|
| `checkEarlyEnd(state)` | 提前下台判定（支持率<15 / 国库+经济崩溃 / 稳定<10） |
| `checkElection(state)` | 任期 48 月届满大选（均值≥50 且支持率≥40 即连任） |
| `calcGrade(metrics)` | 计算评级 S/A/B/C/D |
| `gradeNarrative(grade, term)` | 评级叙事文本 |

### `src/engine/legacySystem.ts` — 历史遗产
| 函数 | 作用 |
|---|---|
| `generateLegacy(state)` | 生成历史定性、教科书评估、退任头条、回忆录标题与畅销度、历史标签、肖像画描述 |

---

## 4. 页面路由与导航

### 一级路由（`App.tsx`）
基于 `screen` 状态切换，配 `motion/react` 淡入淡出（0.4s）：
- `menu` → `<MainMenu />`
- `game` → `<Game />`
- `ending` → `<Ending />`

### 二级路由（`Game.tsx`）
基于 `gamePhase` 控制开局流程：
- `coalition` → 全屏 `<CoalitionNegotiation />`（隐藏导航与时间控制）
- `cabinet_setup` → 全屏 `<InitialCabinetSetup />`
- `playing` → 标准布局：`<StatusBar />` + `<GameNav />` + 子页面 + `<SideRail />` + `<PendingTodos />` + `<SidePanel />` + `<TimeControl />` + `<Tutorial />`

### 三层导航架构（v1.3 重构）

**首页强制为仪表盘**：进入游戏后第一个看到的就是仪表盘（DashboardPage），而非百科全书。百科全书已改为弹窗形式（见下）。

**顶部主导航（GameNav）**：紧急待办 → 施政工具 → 国政各领域 → 新闻回溯
| 顺序 | 页面 | 快捷键 |
|---|---|---|
| 1 | 仪表盘（首页） | D |
| 2 | 总理 | F |
| 3 | 任务树 | T |
| 4 | 政策树 | P |
| 5 | 改革 | I |
| 6 | 法律（v1.4 新增） | J |
| 7 | 人物谱（v1.4 新增） | K |
| 8 | 外交 | O |
| 9 | 军事 | M |
| 10 | 经济 | E |
| 11 | 社会 | S |
| 12 | 环境 | N |
| 13 | 新闻 | W |

**右侧图标栏（SideRail）**：议会 / 民意 / 内阁 三组，平时仅图标，鼠标悬停展开为图标+文字菜单
| 组 | 子页面 | 快捷键 |
|---|---|---|
| 议会 🏛️ | 议会 / 质询 | L / B |
| 民意 ✉️ | 信件 / 舆论 / 大选 | R / A / X |
| 内阁 👥 | 内阁名单 / 内阁聊天 | V / C |

**右侧滑出面板（SidePanel）**：右侧栏页面以侧栏抽屉形式展示，不再占用全屏。点击右侧栏图标即打开对应面板，半透明遮罩点击关闭。

**右上角"菜单"**：包含百科（弹窗）、存档管理、设置、关于、返回主菜单、退出游戏

**百科全书（v1.3 改为弹窗）**：
- 不再是独立页面，而是 `EncyclopediaModal` 弹窗
- 在右上角菜单中点击"百科"即可弹出，覆盖在当前页面之上
- 关闭弹窗后回到原页面，不打断当前操作
- 支持分类筛选与关键词搜索，每条目可一键跳转对应页面
- 移除了 `EncyclopediaPage` 的页面路由，百科不再占据主导航位置

**全局快捷键**：
- 空格：暂停/恢复时间（有卡牌事件时已自动暂停，空格不切换）
- D/F/T/P/I/J/K/O/M/E/S/N/W：跳转主导航页面（v1.4 新增 J 法律、K 人物谱）
- L/B/R/A/X/V/C：打开对应侧面板（再按一次关闭）

> **注意**：v1.2.2 起，开发者控制台不再通过 `~` / `` ` `` 键呼出。改为右上角"菜单" → "🛠️ 开发者选项" → 二次确认进入。详见 [§23 开发者控制台](#23-开发者控制台)。

---

## 5. 游戏机制总览

| 机制类别 | 数据量 | 关键文件 |
|---|---|---|
| 一级指标 | 6 项 | metrics.ts |
| 二级指标 | 19 项 | metrics.ts |
| 普通事件 | 44+ 个 | events.ts |
| 道德困境 | 7 个 | moralDilemmas.ts |
| 紧急事件 | 7 个 | emergencies.ts |
| 入侵事件 | 3 个 | invasions.ts |
| 倒计时事件 | 5+ 个 | countdownEvents.ts |
| 命运事件 | 动态 | fateEvents.ts |
| 改革方案 | 41+ 个 | initiatives.ts |
| 政策 | 多类多分支 | nationalPolicies.ts |
| **法律组（v1.4）** | **7 组 × 3 档** | **lawsData.ts** |
| **跨系统联动事件（v1.4）** | **10+ 个** | **crossSystemEvents.ts** |
| **个人事件（v1.4）** | **8+ 个** | **personalEvents.ts** |
| 内阁初始职位 | 6 个 | cabinet.ts |
| 内阁候补 | 18 个（6×3） | cabinet.ts |
| 新部门候选 | 6 个 | cabinet.ts |
| 预设新部门 | 8 个 | cabinet.ts |
| 可选执政党 | 6 个 | parties.ts |
| 议会小党 | 2 个 | parties.ts |
| 组阁谈判事件 | 3 个 | parties.ts |
| 质询卡 / 问题 | 6 + 6 | debates.ts |
| 选区信件 | 13 封 | letters.ts |
| 外交照会 | 13 份 | notes.ts |
| 法案类型 | 6 种 | BillVotingDialog.tsx |
| 成就 | 8+ 个 | achievements.ts |
| 议会事件 / 行动 | 4 + 4 + 3 | parliament.ts |
| 大选事件 / 纲领 | 4 + 4 | elections.ts |
| 总理背景 / 特质事件 | 3 + 动态 | pmBackgrounds.ts / pmTraitEvents.ts |
| **人物谱 NPC（v1.4 扩展）** | **15 位 / 6 派系** | **npcs.ts** |
| 国家叙事节拍 | 20+ | nationalStory.ts |
| 环境新闻 | 30+ | ambientNews.ts |
| 外交国家 | 多国 | diplomacy.ts |
| 总理特质 | 5 项数值 | gameStore.ts |
| **宏观经济引擎（v1.4）** | **GDP/失业/通胀** | **simulation.ts** |
| **总理个人生活（v1.4）** | **3 项（家庭/腐败/压力）** | **PMProfilePage.tsx** |

---

## 6. 指标系统

### 一级指标（Metrics，0–100）
| Key | 名称 | 说明 |
|---|---|---|
| `approval` | 民意支持 | 民众对总理的支持率 |
| `treasury` | 国库储备 | 国家财政健康度（每月自然 -1） |
| `economy` | 经济指数 | 工农业与就业综合 |
| `stability` | 社会稳定 | 治安与民族团结 |
| `diplomacy` | 外交关系 | 与大国/邻国/国际组织关系 |
| `prestige` | 个人声望 | 政坛/媒体/历史评价 |

### 二级指标（SecondaryMetrics，19 项）
- **民意**（3）：城市/农村/青年支持率
- **国库**（3）：财政盈余/债务水平/外汇储备
- **经济**（4）：工业产值/农业产值/就业率/通胀率
- **稳定**（3）：犯罪率/抗议频率/民族团结
- **外交**（3）：大国关系/邻国关系/国际组织影响力
- **声望**（3）：政坛威望/媒体评价/历史定位

二级指标由一级指标通过 `deriveSecondary()` 每月推导，分正向/负向。仪表盘指标卡片可点击展开二级细分，以树状结构展示从属关系。

### 指标等级与颜色
| 区间 | 颜色 | 等级 |
|---|---|---|
| 80–100 | 绿 | 极佳 |
| 60–79 | 金 | 良好 |
| 40–59 | 黄 | 平稳 |
| 25–39 | 橙 | 堪忧 |
| 10–24 | 红 | 危险 |
| 0–9 | 深红 | 崩溃 |

### 难度对指标的影响
- **普通难度**：标准加减成
- **困难难度**：加成缩放、惩罚放大，且部分事件的加成/惩罚数值隐藏，需玩家自行判断

---

## 7. 事件系统

### 事件分类
| 类型 | 数量 | 触发方式 | 特点 |
|---|---|---|---|
| 普通事件 | 44+ | 每 25 天加权随机 | 支持冷却（240 天）、事件链、一次性 |
| 道德困境 | 7 | 嵌入普通事件池 | 含"主动辞职"等 endsGame 选项 |
| 紧急事件 | 7 | 每 15 天检查指标阈值 | 可重复、有 cooldown |
| 入侵事件 | 3 | 复杂条件触发，每 30 天检查 | 多阶段（phases）选项 |
| 倒计时事件 | 5+ | 实时秒数倒计时 | 存在时强制暂停游戏时间，必须当场决断 |
| 命运事件 | 动态 | 特定条件 | 长期影响 |
| 延迟后果 | 动态 | 定时炸弹到 triggerDay | 触发 breaking news |

### 事件收纳篮（pendingEvents）
- **所有事件不再阻塞时间**，进入队列后玩家有 **7 天**决策窗口
- 超时按 `defaultOptionId` 自动决策
- 同时最多 3 个待处理事件
- 通过 `EventBasket` 组件查看，`EventPopup` 打开详情

### 事件链机制
- 选项携带 `chainId` + `chainDelay`（默认 3 月）
- 存入 `pendingChains`，到期由 `pickEvent` 优先触发
- 用于构建剧情连贯性（如：贸易协定 → 后续报复 → 危机升级）

### 卡牌事件（CardEvent）
议会质询等关键事件以卡牌形式呈现：
- 触发时自动暂停时间
- 底部弹出 `HandBar` 手牌栏
- 拖拽卡牌至中央槽位打出
- 解决后自动恢复时间速度（v1.2 修复）

---

## 8. 改革系统

### 改革字段（Initiative）
- `cost`：国库消耗
- `duration`：持续月数
- `perTurnEffects`：每回合效果
- `completionEffects`：完成时一次性效果
- `completionSecondaryEffects`：完成时二级指标效果
- `prerequisites`：指标前提条件
- `requiresInitiative`：前置改革（必须先完成）
- `politicalCapitalCost`：政治资本消耗
- `factionEffects`：派系好感度影响
- `radical`：是否为激进改革（改变国家路径）
- `once`：仅可执行一次
- `unlocksPolicies`：完成后解锁的政策分支

### 树状图布局（v1.3 重构：从上到下 + 深层节点）

**关键变化**：
- **方向**：从原来的"从左到右"水平树状，改为"从上到下"垂直树状（`InitiativesPage.tsx`）
- **深度**：从原来只有 L0、L1 两阶，扩展至 **L0~L5+** 的深层节点树
- **层级标签**：每层左侧显示 `L0 根改革`、`L1 第一阶`、`L2 第二阶`… 标签
- **连接线**：父节点与子节点之间用 SVG 贝塞尔曲线连接，未解锁的前置链用虚线表示
- **多前置支持**：子节点可有多个 `requiresInitiative`，连接线从所有父节点汇聚

**深度计算**：由 `getInitiativeDepth(initiative)` 函数动态计算，沿 `requiresInitiative` 链路向上追溯。

### 改革链（v1.3 大幅扩充，深度扩展至 L5）

**经济链（L0→L5）**
- L0 税制优化 → L1 产业升级 → L2 科技创新基金 → L3 产业研究整合 → L4 国家实验室 → L5 全球人才引进
- L0 数字基础设施（与科技创新基金并列，可作为国家人工智能战略的第二前置）
- L2 国家人工智能战略（需科技创新基金 + 数字基础设施双前置）
- L0 城市更新计划 → L1 国家级标志性工程

**社会链（L0→L4）**
- L0 全民医疗覆盖计划 → L1 疾控监测网络 → L2 战略医疗储备
- L0 义务教育质量提升 → L1 一流大学建设 → L2 产业研究整合（与经济链交汇）
- L0 警务体系现代化 → L1 国家安全情报网

**环境链（L0→L2）**
- L0 绿色能源转型 → L1 能源多元化战略 → L2 战略石油储备

**政治体制链（L0→L4）**
- L0 司法审查制度 → L1 陪审团制度试点 → L2 司法独立性改革 → L3 法官任命机制 → L4 司法透明化

**军事链（L0→L5）**
- L0 国防现代化 → L1 强军战略 → L2 军民融合 → L3 国防科技 → L4 太空军用化
- L0 裁军与和平路线（与强军战略互斥分支）

**文化链（L0→L2）**
- L0 文化复兴运动 → L1 一流大学建设（双前置：文化复兴 + 义务教育）

### 改革流程
1. 玩家在改革页面选择改革 → `startInitiative`
2. 扣国库与政治资本、检查前提、加入 `activeInitiatives`
3. 每月 `advanceMonth`：应用 perTurnEffects，elapsed++
4. elapsed ≥ duration → 应用 completionEffects，移入 `completedInitiatives`
5. 完成改革 ≥5 个 → 解锁"改革先锋"历史标签
6. 部分改革完成后会触发 `completionDelayedConsequence`（延迟后果，如核计划→国际制裁）

### 改革与政策联动
- 改革的 `unlocksPolicies` 字段可解锁政策树中的特定分支
- 政策的 `unlockedByInitiative` 字段反向引用改革
- 玩家需在改革树中推进到足够深度才能解锁高级政策

---

## 9. 政策系统

### 政策树（nationalPolicies.ts）
- 按类别分组：经济 / 社会 / 外交 / 军事 / 环境 / 政治体制
- 每类同时生效一项政策
- 切换政策需付出代价（政治资本、稳定等）
- 政策有前置链：必须启用过某些政策才能解锁后续
- 部分政策需先完成特定改革才能解锁（`unlockedByInitiative`）

### 政策字段
- `category`：所属类别
- `effects`：生效时持续指标影响
- `prerequisites`：启用前置条件（指标阈值）
- `requiresPolicy`：前置政策（必须先启用）
- `cost`：切换代价
- `unlockedByInitiative`：由改革解锁

---

## 10. 内阁系统

### 内阁成员（CabinetMember）
- 6 个初始职位：财政 / 外交 / 内政 / 国防 / 经济 / 总理府秘书长
- 字段：`loyalty`（忠诚度）、`specialty`（专长指标）、`advice`（建议）、`bonuses`（六项指标加成）

### 加成机制
每月结算时按 `value × (loyalty / 100)` 缩放应用——忠诚度越低，实际加成越少。

### 内阁改组（三模式）
1. **更换现任成员**：从 `REPLACEMENT_CANDIDATES`（6 角色 × 3 候补）中选择继任者，含加成对比
2. **设立全新部门**：
   - 自定义部门名称（最多 12 字）或从 8 个预设中选择（科技/教育/环保/卫生/文化/交通/能源/数字事务）
   - 从 `NEW_DEPARTMENT_CANDIDATES`（6 人）中任命首长
   - 已存在的部门名会禁用
   - 设立效果：声望 +3、民意 +1、稳定 -1
3. **解职**：直接移除低忠诚度成员

### 内阁聊天系统
- 每 75 天部长会主动私信，附带选项需回应
- 未回应的部长消息会在右侧栏显示红点
- 内阁名单页可点击"与该部长聊天"跳转到聊天面板

### NPC 记忆系统
- `recordNPCAction(npcId, type, description)` 记录 5 类行为：
  - `promoted`（晋升）、`betrayed`（背叛）、`dismissed`（解职）、`helped`（帮助）、`insulted`（侮辱）
- 影响 NPC tone：`friendly` / `neutral` / `resentful` / `hostile`
- 5 个固定 NPC：反对党领袖、中间派/左翼/右翼党魁、党内竞争对手
- NPC 性格 5 种：`idealist` / `pragmatist` / `hardliner` / `moderate` / `opportunist`

---

## 11. 议会系统

### 议会基础（ParliamentState）
- **共 100 席**，初始执政党 55 席、信任度 65
- 总统：60% 概率同党、40% 概率反对党，初始关系 60

### 关键常量
| 常量 | 值 | 说明 |
|---|---|---|
| `TERM_LENGTH_MONTHS` | 48 | 每届任期 4 年 |
| `MAX_DISSOLUTIONS_PER_TERM` | 1 | 任期内最多解散 1 次 |
| `DISSOLUTION_COOLDOWN_MONTHS` | 18 | 解散后冷却 1.5 年 |
| PMQs 自动触发周期 | 60 天 | 每 60 天自动触发议会质询 |
| PMQs 手动冷却 | 30 天 | 手动发起需距上次触发满 30 天 |

### 4 个议会行动
| 行动 | 条件 | 效果 |
|---|---|---|
| `dissolve` 解散议会 | 任期 ≥6 月、未达上限 | 重新选举产生新席位 |
| `qa_session` 质询 | 无 | 提升信任度 |
| `vote_confidence` 信任投票 | confidence ≥40 | 提升席位 |
| `propose_law` 立法 | 席位 ≥40 | 推进政策 |

### 法案表决（密室政治）
议会页可"提交法案表决"，6 种法案类型可选：
- 经济刺激法案、社会福利扩展法案、财政改革法案
- 国家安全强化法案、外交授权法案、教育改革法案

**流程**：
1. 选择法案类型 → 进入投票计算
2. 初始票数基于执政联盟席位
3. 玩家有 `MAX_BACKROOM_ACTIONS` 次密室行动机会：
   - 拉票（lobby）：针对特定党派
   - 施压（pressure）：以资源换票
   - 妥协（compromise）：让渡部分利益
4. 赞成票过半（PASS_THRESHOLD）→ 法案通过，立即生效
5. 失败 → 扣声望

### 大选计算
- **常规大选**（48 月届满）：`approval×0.4 + economy×0.25 + stability×0.2 + prestige×0.15`，±20 席波动
- **提前大选**：基于 `approval - riskIndex×0.3`
- **连任条件**：均值 ≥50 且支持率 ≥40
- **提前大选冷却**：v1.2 修复了可重复点击的 bug，现在有冷却限制

### 不信任投票
当民意或议会信任度过低时触发，3 个应对选项：
1. 斡旋谈判
2. 电视讲话
3. 接受结果，体面下台（`endsGame: true` → 直接进入结算）

---

## 12. 总理系统与性格特质

### 3 个背景（PMBackground）
| ID | 名称 | 加成倾向 |
|---|---|---|
| `legal_expert` | 律政专家 | rhetoric +10 |
| `union_representative` | 工会代表 | partyPrestige +10 |
| `political_dynasty` | 政治世家 | politicalCapital +15 |

### 总理性格特质（PMTraits，数值滑块 0-100）

v1.2 重构为 5 项数值滑块，**v1.3 全面实装特质与事件/机制的深度联动**。游戏中发生的各类事件会实时改变这些数值（通过 `updatePMTraitsNumeric` action），数值又通过 `applyTraitScaling` 反哺游戏效果。

| 特质 | 数值效果 | 低值惩罚 | 高值加成 |
|---|---|---|---|
| **健康** health | 影响**每月行动次数**（`getMaxActionsPerTurn`） | **< 30 时每月行动次数降为 2**（正常 3），并可能触发病休事件（`healthEventActive`） | ≥ 80 时无额外加成，但抵御病休 |
| **魅力** charisma | 影响**民调回升速度**与**外交好感度加成** | < 30 时民调回升缓慢，外交谈判成功率降低 | > 70 时民调回升 +10%，外交好感 +5 |
| **果断** decisiveness | 影响**紧急事件响应速度**与**决策成功率** | **< 30 时强制施加"优柔寡断惩罚"**：所有正向效果 ×0.8 | > 70 时正向效果 ×1.1，负向效果 ×0.9 |
| **韧性** resilience | 代表抗压能力，**直接降低连续负面事件造成的连锁损失** | < 30 时连续负面事件（≥2 次）损失不衰减 | > 70 时连续负面事件损失按 `(100-resilience)/100` 缩放 |
| **道德** integrity | 代表廉洁程度 | **< 30 时增加腐败/丑闻触发概率 +15%**，**提高黑料卡使用代价 +50%** | > 70 时降低丑闻触发概率，黑料卡代价降低 |

### 特质缩放实现（`applyTraitScaling`）

位于 `src/engine/eventEngine.ts`，在以下调用点应用：
- `gameStore.chooseActionDialogOption`（总理日常行动结算）
- `gameStore.resolvePendingEventInternal`（事件选项结算）
- `eventEngine.resolveOption`（事件选项结算）

**输入**：原始 effects（Partial<Metrics>）+ 当前 GameState
**输出**：`{ scaledEffects, isNegative }`

**缩放逻辑**：
1. 计算 effects 净值，判定 isNegative
2. 若 isNegative 且 `consecutiveNegativeEvents >= 2`：按 `(100 - resilience) / 100` 缩放所有负向数值（韧性越高，损失越小）
3. 若 `decisiveness > 70`：正向 ×1.1，负向 ×0.9
4. 若 `decisiveness < 30`（优柔寡断惩罚）：正向 ×0.8
5. 返回缩放后的 effects 与 isNegative 标志（用于更新 `consecutiveNegativeEvents`）

### 行动力与连续负面事件

| 字段 | 作用 | 重置时机 |
|---|---|---|
| `actionsThisTurn` | 本月剩余行动次数 | 每月 `advanceMonth` 重置为 `getMaxActionsPerTurn(state)` |
| `consecutiveNegativeEvents` | 连续负面事件计数 | 每月 `advanceMonth` 清零；触发正向事件时也清零 |
| `healthEventActive` | 病休状态（v1.3 新增持久化） | 由 `checkPMTraitEvent` 在每月检查时维护 |

**健康受限机制**：
- 健康 < 30 时 `getMaxActionsPerTurn` 返回 2（而非 3）
- 仪表盘"总理行动"模块会显示 `⚠️ 健康欠佳，本月行动次数受限（2/3）` 警告
- 健康 < 30 时有概率触发病休事件：总理病休两周，副总理代行职权，当月行动次数额外 -1
- 病休期间 `healthEventActive = true`，事件暂停推进

### 总理数值（PMStats，默认 50）
| 字段 | 作用 |
|---|---|
| `politicalCapital` 政治资本 | 核心消耗资源，改革/行动需要（<30 时无法执行多数行动） |
| `partyPrestige` 党内威望 | 低于阈值触发党内挑战 |
| `rhetoric` 辩论技巧 | 影响质询成功率与密室游说威胁成功率 |
| `riskIndex` 风险指数 | 负面事件积累程度，影响密室政治威胁成功率 |

### 4 个日常行动
| 行动 | 冷却 | 效果 |
|---|---|---|
| `speech` 发表演说 | 短期 | approval +5, prestige +2 |
| `inspect` 视察地方 | 中期 | approval +3, stability +3 |
| `diplomacy_call` 外交热线 | 中期 | diplomacy +5 |
| `rest` 日常政务 | 短期 | politicalCapital +5 |

**行动受限**：
- 每月行动次数受健康值限制（健康 ≥30：3 次/月；健康 <30：2 次/月）
- 政治资本 < 30 时无法执行多数行动
- 行动效果受 `applyTraitScaling` 缩放（果断 >70 放大，<30 缩小）

### 总理档案页（PMProfilePage）
- 总理徽章：SVG 人像剪影 + 总理名首字浮层（v1.2 修复跨平台显示，不再使用 emoji 🪪）
- 展示性格特质、执政资源、背景与所属政党
- v1.3：特质滑块旁显示当前数值对应的加成/惩罚说明

---

## 13. 突击新闻发布会（Minigame）

**组件**：`src/components/PressConferenceMinigame.tsx`
**触发条件**：当总理陷入大丑闻（`pressConferenceSeverity ≥ 60`）时，不再走普通公关卡，而是直接切入新闻发布会现场。

### 触发与入口
- 丑闻严重度 ≥ 60 时由 `startPressConference(severity)` 触发
- 触发时**游戏时间自动暂停**（记录 `previousTimeSpeed`，结束后恢复）
- 全屏覆盖，强制玩家应对

### 交互逻辑
1. **5 轮提问**：记者连续抛出 5 个犀利问题
2. **3 秒倒计时**：每题有 3 秒倒计时，超时自动选择"避重就轻"
3. **4 个交互选项**：

| 选项 | 行为 | 效果 |
|---|---|---|
| 避重就轻 | 玩家不正面回答 | 严重度小幅回升，民意略降 |
| 眼神锁定友好记者 | 主动点名友好记者提问 | 魅力 >60 时成功率提升，民意回升；失败则被嘲弄 |
| 直接怒斥记者 | 强硬回击 | 果断 >70 时威慑成功，声望提升；失败则舆论反弹 |
| 离席摔门而去 | 拒绝回应 | 立即结束发布会，严重度保持高位，声望大跌 |

### 特质影响
- **魅力**：影响"眼神锁定友好记者"的成功率与效果
- **果断**：影响"怒斥记者"的威慑效果
- **道德**：道德过低时记者更咄咄逼人（提问更犀利，倒计时更短）

### 结算（`endPressConference`）
- 输入：`{ finalSeverity, approvalDelta, prestigeDelta }`
- 应用 metrics 变化（受 `applyTraitScaling` 缩放）
- 推送新闻条目（根据最终严重度选择正面/负面/中性标题）
- 恢复 `previousTimeSpeed`

### 存档兼容
- `pressConferenceOpen` / `pressConferenceSeverity` 已持久化到 GameState
- 读档时 `migrateGameState` 强制 `pressConferenceOpen = false`，防止卡在 minigame

---

## 14. 深夜官邸密室游说棋盘（Minigame）

**组件**：`src/components/BackroomLobbyMinigame.tsx`
**触发条件**：玩家在议会/总理页主动切入"密室游说"入口，或特定事件触发。

### 触发与入口
- 由 `startBackroomLobby()` 触发
- 触发时**游戏时间自动暂停**（与新闻发布会一致）
- 全屏覆盖，呈现 5×5 棋盘

### 棋盘与走位
- **5×5 网格**：代表官邸深夜的会客厅布局
- **玩家初始位置**：左上角 (0, 0)
- **7 位利益集团代表**：军火商、工会领袖、媒体大鳄等，随机分布在棋盘上
- **步数限制**：默认 8 步（健康 < 30 时 -2，即 6 步）
- **操作**：
  - `W/A/S/D` 或方向键：移动玩家
  - `E`：与相邻代表发起谈判

### 谈判机制
当玩家与某位代表相邻时，按 `E` 发起谈判，可选 3 种策略：

| 策略 | 成功率公式 | 消耗 | 成功效果 | 失败效果 |
|---|---|---|---|---|
| 承诺 promise | `base + (charisma - 50) × 0.004` | 国库 -5, 政治资本 -3 | 影响力 +20 | 影响力 0，代表离开 |
| 威胁 threaten | `base + (rhetoric - 50) × 0.003` | 风险指数 +8 | 影响力 +25 | 引爆丑闻，触发新闻发布会 |
| 利益交换 interest | `base + (charisma - 50) × 0.001` | 政治资本 -5 | 影响力 +15 | 影响力 0 |

### 胜利条件
- 总影响力 ≥ 60：**大幅推进议程**（议会信任度 +10，相关指标 +5）
- 总影响力 30-59：**部分成功**（议会信任度 +5）
- 总影响力 < 30 或步数耗尽：**失败**（声望 -3）

### 特质影响
- **魅力**：影响"承诺"与"利益交换"成功率
- **辩论技巧**（PMStats）：影响"威胁"成功率
- **道德**：道德过低时威胁成功率提升但失败后果更严重
- **健康**：健康 < 30 时步数 -2

### 结算（`endBackroomLobby`）
- 输入：`{ success, totalInfluence, bribedReps, usedThreaten }`
- 消耗国库/政治资本/风险指数
- 应用成败效果到 metrics 与 parliament.confidence
- 若 `usedThreaten` 且有失败，可能触发后续新闻发布会
- 恢复 `previousTimeSpeed`

### 存档兼容
- `backroomLobbyOpen` 已持久化到 GameState（v1.3 新增）
- 读档时 `migrateGameState` 强制 `backroomLobbyOpen = false`，防止卡在 minigame

---

## 15. 质询辩论 / 信件 / 照会

### 议会质询（DebateCard）
- **6 张质询卡**：强硬反击、转移话题、妥协让步、归咎前任、数据轰炸、情感诉求
- **6 个质询问题**：经济/社会/外交/财政/民意/声望
- **触发**：每 60 天检查一次（v1.2 修复，使用 `lastPmqsTriggerDay` 追踪，避免无限弹窗）
- **手动触发**：冷却 30 天
- **机制**：每张卡依赖不同 PMStats，消耗资源，有 baseSuccessRate，成功/失败应用不同效果
- **暂停**：质询触发时自动暂停，解决后自动恢复（v1.2 修复 `previousTimeSpeed` 恢复）

### 选区信件（ConstituencyLetter）
- **13 封信**（v1.2 扩充）：农业/住房/工商/养老/环保/教育/医疗/交通/能源/就业/税收/治安/文化
- **触发**：每 90 天生成，上限提升
- **机制**：每封 3 个回复选项，影响 approval/treasury/economy/stability/prestige
- **菜单红点**：未读信件在菜单栏与右侧民意图标显示红点

### 外交照会（DiplomaticNote）
- **13 份照会**（v1.2 扩充）：自贸/科技/军援/贷款/难民/环境/文化/引渡/关税/能源/卫生/教育/劳工
- **触发**：每 120 天生成
- **机制**：acceptEffects 与 rejectEffects 二选一
- **菜单红点**：未读照会在菜单栏与右侧民意图标显示红点

---

## 16. 外交系统

### 外交页（DiplomacyPage）
- 展示与多国关系（0-100）
- 可执行外交行动：缔结条约、贸易协定、制裁、援助等
- 每项行动有冷却
- 关系过低可能触发入侵事件

### 入侵事件
- 每 30 天在 `advanceDay` 中检查（v1.2 修复，原仅旧回合制 `nextMonth` 检查）
- 复杂条件触发：关系、军事、地缘等综合判定
- 多阶段选项（phases）
- 宣战冷却：999 月哨兵值 = "每国每届仅一次"（v1.2 显示为"每国一次"）

---

## 17. 组阁要求追踪机制

### 设计目的
玩家所选执政党的组阁要求不再是"开局即满足"，而是需要在后续执政中**主动落实**，否则面临辞职压力。

### GameState 字段
| 字段 | 初始值 | 作用 |
|---|---|---|
| `playerPartyId` | 所选党 ID | 追踪玩家执政党 |
| `partyPatience` | 100 | 执政党耐心值（0–100） |
| `lastUltimatumTurn` | 0 | 上次触发最后通牒的回合 |

### 各党组阁要求
| 党派 | 组阁要求 |
|---|---|
| 进步联盟 | 推进绿色能源转型改革；扩大公共医疗投入 |
| 国民和谐党 | 维持财政赤字可控；推动产业升级改革 |
| 自由人民党 | 通过减税或放松管制改革；增加国防预算或强化治安 |
| 生态未来党 | 要求通过环保法案；要求设立环境法庭 |
| 劳动者联合 | 要求提高最低工资；要求修订劳动法 |
| 自由民主党 | 要求减税；要求减少政府干预 |

### 耐心值变化
- **每月自然衰减**：-1
- **改革契合回馈**：完成与组阁要求关键词匹配的改革时 +8
- **触发最后通牒**：当 `partyPatience < 30` 且距上次通牒 ≥6 个月时

### 最后通牒事件
3 个选项：
1. **承诺立即推进相关改革**：耐心回升，声望 -4，稳定 +2
2. **做出部分让步**：稳定 +3，民意 -3，声望 -2
3. **主动辞职**（`endsGame: true`）：直接进入结算页面

---

## 18. 成就系统

| ID | 名称 | 解锁条件 |
|---|---|---|
| `ach_first` | 初登大宝 | 完成首届任期 |
| `ach_reelect` | 连任成功 | 连任一届 |
| `ach_three_terms` | 三朝元老 | 连任三届 |
| `ach_full_house` | 满堂喝彩 | 六项指标同时 ≥80 |
| `ach_survivor` | 不倒翁 | 民意曾跌至 <20 后扭转至 >50 |
| `ach_economy_miracle` | 经济奇迹 | 经济指标达到 100 |
| `ach_diplomacy_master` | 外交大师 | 外交指标达到 100 |
| `ach_centurion` | 长青总理 | 执政超过 100 回合 |

### 成就判定（v1.2 重构）
- 使用持久状态追踪而非新闻标题分析
- 例：`不倒翁` 现使用 `hadLowApproval` 标志记录民意是否曾跌破 20，需后续回升至 50+ 才解锁
- 通过 `checkAchievements` 在状态变更时自动检测解锁
- 解锁时弹出 `AchievementToast` 通知
- 困难难度下阈值更高，更难达成

---

## 19. 结局系统

### 5 档评级（EndingGrade）
| 评级 | 均值阈值 | 评价 |
|---|---|---|
| **S** | ≥85 | 青史留名 |
| **A** | ≥70 | 卓有成效 |
| **B** | ≥55 | 功过相当 |
| **C** | ≥40 | 平庸乏味 |
| **D** | <40 | 遗臭万年 |

### 结束游戏路径
1. **被动 — 任期届满大选失败**：48 月到期 → `checkElection` → 结算
2. **被动 — 提前下台**（`checkEarlyEnd`）：
   - 支持率 <15 → 不信任投票下台
   - 国库 <5 且经济 <20 → 主权债务危机
   - 稳定 <10 → 大规模骚乱辞职
3. **主动 — 玩家点击辞职**：`endGameEarly` → 文案"总理主动辞职，提前结束任期。"
4. **事件选项触发**：选项标记 `endsGame: true`（如"主动辞职"、"接受结果体面下台"、执政党最后通牒中的"主动辞职"）

### 历史遗产（LegacyAssessment）
结算页面根据最终状态生成：
- **historicalTitle**：历史定性标题（如"缔造奇迹的国父"、"摇摇欲坠的妥协大师"、"遗臭万年的昏聩之主"）
- **textbookEvaluation**：教科书评估段落（含执政年数、连任届数）
- **retirementHeadline + headlineSubtitle**：退任头条与副标题
- **memoirTitle + memoirPopularity**：回忆录标题与畅销度（0–100）
- **historicalTags**：3–5 个历史标签（经济繁荣/万民拥戴/国泰民安/外交辉煌/长期执政/改革先锋/国有化推手/市场化旗手/被迫下台/主动请辞）
- **portraitDescription**：肖像画描述（S 档金色画框挂正厅，D 档被移出仓库积灰）

---

## 20. 时间与节奏配置

### 时间速度（TimeControl.tsx）
5 档速度 + 暂停：

| 档位 | 间隔 (ms) | 实际速度 |
|---|---|---|
| ⏸ 0 | — | 暂停 |
| Ⅰ 1 | 4000 | ≈120 秒/月 |
| Ⅱ 2 | 2500 | ≈75 秒/月 |
| Ⅲ 3 | 1500 | ≈45 秒/月（标准） |
| Ⅳ 4 | 1000 | ≈30 秒/月 |
| Ⅴ 5 | 666 | **≈20 秒/月（极速）** |

- **空格键**：切换暂停（v1.2 修复，原 Game.tsx 与 TimeControl.tsx 双重监听导致双切换）
- **卡牌事件存在时**：自动暂停，解决后自动恢复（v1.2 修复 `previousTimeSpeed` 恢复）
- **倒计时事件存在时**：强制暂停游戏时间，必须当场决断
- **自动存档**：每 15 分钟一次（仅 `gamePhase === 'playing'` 时）；v1.2 移除了事件决策后的自动存档

### 事件触发周期（advanceDay 内）
| 周期 | 触发内容 | 限制 |
|---|---|---|
| 每 25 天 | 普通事件抽取 | pendingEvents < 3 |
| 每 15 天 | 紧急事件检查 | 未触发过的 |
| 每 30 天 | 入侵事件检查 | 复杂条件 |
| 每 60 天 | 议会质询辩论（自动） | !activeCardEvent，使用 `lastPmqsTriggerDay` 追踪 |
| 每 75 天 | 内阁部长主动私信 | — |
| 每 90 天 | 选区信件生成 | — |
| 每 120 天 | 外交照会生成 | — |
| 70% 概率 | 议会/总统行动后触发事件 | — |

### 事件超时
- PendingEvent 默认 `deadlineDay = triggeredDay + 7`（7 天后自动决策）
- 单事件冷却期：240 天（`EVENT_COOLDOWN_DAYS`）

---

## 21. 存档系统

### 存档目录
- **开发模式**：`<项目根>/saves/`
- **生产环境**：`<userData>/saves/`
- 自动存档与手动存档**存放在同一目录**

### 存档文件命名
| 类型 | 前缀 | 示例 |
|---|---|---|
| 手动存档 | `save_` | `save_20260726_143022.json` |
| 自动存档 | `auto_` | `auto_20260726_143152.json` |

### 存档数据结构（SaveData）
```typescript
{
  version: '1.4.0',
  savedAt: '2026-07-28T14:30:22.000Z',
  gameState: { /* 完整 GameState，含性格特质/Minigame 状态/扩展改革树/v1.4 法律/个人生活/宏观指标等 */ },
  saveName: '总理名 - 2026年7月',
  isAuto: false
}
```

### v1.3 新增持久化字段

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `pmTraitsNumeric` | PMTraits | DEFAULT_PM_TRAITS | 5 项性格特质（健康/魅力/果断/韧性/道德） |
| `actionsThisTurn` | number | 0 | 本月剩余行动次数 |
| `consecutiveNegativeEvents` | number | 0 | 连续负面事件计数（韧性缩放用） |
| `pressConferenceOpen` | boolean | false | 突击新闻发布会 minigame 状态 |
| `pressConferenceSeverity` | number | 0 | 当前丑闻严重度 |
| `backroomLobbyOpen` | boolean | false | 密室游说 minigame 状态（v1.3 新增到 GameState） |
| `healthEventActive` | boolean | false | 病休状态（v1.3 新增） |

### v1.4 新增持久化字段

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `enactedLaws` | `Record<string, number>` | `{}` | 各法律组当前档位（0=未立法，1/2/3 对应三档） |
| `personalLife` | `PersonalLife` | `{ familyRelation:80, corruption:0, stress:20 }` | 总理个人生活三维（家庭关系/腐败度/压力值） |
| `npcDisposition` | `Record<string, number>` | 各 NPC 初始值 | 15 位 NPC 对总理的好感度（0-100） |
| `npcInteractionLog` | `NPCInteraction[]` | `[]` | 与 NPC 的互动历史记录 |
| `macroEconomy` | `MacroEconomy` | 见 simulation.ts | 宏观经济状态（GDP 总量/月增长率/失业率/通胀指数） |
| `crossSystemFlags` | `Record<string, boolean>` | `{}` | 跨系统联动事件的触发标志位 |
| `lastCrossSystemCheckDay` | number | 0 | 上次跨系统事件检查天数 |

**改革树扩展**：改革树数据结构未变（`Initiative` 接口不变），仅 `requiresInitiative` 链路扩展至 L5 深度，旧存档无需迁移改革数据。

### 存档迁移函数（v1.4 扩展 `migrateGameState`）

位于 `src/hooks/useSaveGame.ts`，在 `loadSave` 与 `loadSaveById` 中调用。

**v1.3 基础职责**：
- 强制关闭所有 minigame 状态（`pressConferenceOpen = false`、`backroomLobbyOpen = false`），防止读档后卡在 minigame
- 清除可能卡住的全屏弹窗（`actionDialog`、`showEventBasket`、`activePendingEventId`、`breakingNews`、`currentCountdown`、`activeCardEvent`）
- 补齐 `healthEventActive`（旧存档无此字段时为 false）
- 强制 `timeSpeed = 0`（读档后暂停，由玩家手动恢复）

**v1.4 扩展职责**：
- **补齐 `enactedLaws`**：旧存档无此字段时初始化为 `{}`（无任何法律生效）
- **补齐 `personalLife`**：旧存档无此字段时使用默认值（家庭关系 80 / 腐败 0 / 压力 20）
- **补齐 `npcDisposition`**：旧存档无此字段时按 `NPCS` 数据的 `initialDisposition` 初始化全部 15 位 NPC
- **补齐 `npcInteractionLog`**：旧存档无此字段时为空数组
- **补齐 `macroEconomy`**：旧存档无此字段时基于当前 `metrics.economy` 推导初始宏观值
- **补齐 `crossSystemFlags` 与 `lastCrossSystemCheckDay`**：默认 `false` / `0`

**字段级兜底**：纯数据缺失（如 `pmTraitsNumeric`、`actionsThisTurn` 等）由 `gameStore.loadGame` 中的 `??` 操作符兜底，无需迁移函数处理。

**版本兼容**：
- v1.1 → v1.4：自动迁移，所有新增字段均补默认值
- v1.2 → v1.4：自动迁移，minigame 状态强制关闭
- v1.3 → v1.4：自动迁移，仅补 v1.4 新增的法律/个人生活/NPC/宏观/跨系统字段

### 存档元信息（SaveMeta）
| 字段 | 说明 |
|---|---|
| `saveId` | 存档 ID（文件名去 .json） |
| `fileName` | 文件名 |
| `savedAt` | 保存时间 ISO |
| `saveName` | 显示名（自动存档前缀 `[自动]`） |
| `pmName` | 总理名 |
| `term` / `turn` / `year` / `month` / `day` | 游戏内时间 |
| `metrics` | 六项指标快照 |
| `fileSize` | 文件大小 |
| `isAuto` | 是否为自动存档 |

### 自动存档机制
- **触发**：由 `TimeControl` 组件的 `setInterval` 触发
- **间隔可配置**：在设置中可调整 1–60 分钟（默认 15 分钟），存于 `settingsStore.autoSaveIntervalMinutes`
- **条件**：仅当 `gamePhase === 'playing'` 时
- **命名**：`[自动] 总理名 - 年月日`
- **清理**：自动存档仅保留最近 **5 个**，超出自动删除最旧的
- **UI 标识**：存档列表中自动存档显示蓝色"自动"徽章
- **v1.2 修复**：移除了事件决策后的自动存档，避免频繁打断；保留每 15 分钟定时存档与手动存档

### 存档操作（通过 GameMenu 菜单）
| 操作 | 说明 |
|---|---|
| 手动存档 | 输入自定义名称或使用默认名；成功后更新 `settingsStore.lastManualSaveAt` |
| 快速存档 | 一键存档（默认名加 "(快速)" 后缀），同样更新 `lastManualSaveAt` |
| 读取存档 | 从列表中选择任意存档（含自动）加载 |
| 覆盖存档 | 用当前状态覆盖指定存档；同样更新 `lastManualSaveAt` |
| 重命名存档 | 修改任意存档的显示名 |
| 删除存档 | 删除指定存档（含自动存档） |
| 返回主菜单 | 从游戏内返回主菜单 |
| 退出游戏 | 关闭窗口（先提示保存） |
| 关于 | 显示版本、开发者、游戏简介 |

### 上次手动存档时间（v1.2.2 新增）
- `settingsStore.lastManualSaveAt` 记录最近一次**手动存档**（含快速存档与覆盖）的 ISO 时间戳
- **自动存档不更新**此字段，便于区分"主动存档"与"系统存档"
- 在游戏内菜单"设置"标签页底部显示为"⏱ 上次手动存档：YYYY-MM-DD HH:MM"提示
- 持久化到 localStorage，跨会话保留

### 存档与开发者控制台的关系
- 开发者控制台修改 `turn` 后，**不会自动存档**——玩家需手动触发存档以保留修改后的状态
- 经开发者控制台修改过的存档在加载时无特殊标记，但加载后会强制 `devConsoleOpen = false`，避免误操作链

### IPC API（electron/main.ts）
| 通道 | 作用 |
|---|---|
| `save:has` | 检查是否有任意存档（含自动） |
| `save:load` | 读取最新存档（手动优先） |
| `save:write` | 写入新存档（支持 isAuto 参数） |
| `save:delete` | 删除最新手动存档（兼容旧版） |
| `save:list` | 列出所有存档（含 isAuto 字段） |
| `save:loadById` | 读取指定 ID 存档 |
| `save:deleteById` | 删除指定 ID 存档 |
| `save:rename` | 重命名指定存档 |
| `dialog:confirm` | 原生确认对话框 |

### 桌面图标（v1.3 修复）
- `electron-builder.yml` 配置 NSIS 安装器图标（Windows）与 .icns 图标（macOS）
- `electron/main.ts` 中 `BrowserWindow` 显式设置 `icon` 选项：
  - Windows：使用 `src/icon/icon.ico`
  - macOS：使用 `src/icon/icon.icns`
  - Linux/其他：使用 `src/icon/icon.png`
- `build/installer.nsh` 安装后刷新 Windows 图标缓存
- 解决安装后桌面图标与任务栏图标仍为 Electron 默认的问题

---

## 22. 新手教程

### 教程组件（Tutorial.tsx）
v1.2 重构为**引导式教程**，v1.3 扩充新机制引导步骤，**v1.4 新增 7 步覆盖法律/NPC/军事/经济/社会/环境/个人生活系统**：

**核心特性**：
- 27 步骤，覆盖所有核心页面与 v1.3/v1.4 新增机制
- 每点"下一步"自动跳转到对应页面（议会/民意/内阁系列走侧面板）
- 用 `clip-path` 遮罩高亮 CSS 选择器定位的元素
- 高亮元素配呼吸边框 + "👉 提示"标签
- 教程框固定在屏幕底部居中，全程不关，不遮挡主内容
- 进度条 + 步骤指示器
- 支持"上一步"、"跳过教程"

**教程步骤**（v1.4 共 27 步）：
1. 欢迎来到宰执春秋
2. **仪表盘 · 首页**（强调：进入游戏第一个看到的就是仪表盘，非百科；高亮指标卡片）
3. **节令时序 · 左下角叙事弹窗**（v1.3 新增；高亮 `[data-story-beat-toast]` 元素；介绍凝神片刻/提笔记下/召人商议三个互动选项）
4. 总理档案（高亮性格特质）
5. 改革树（高亮改革节点）
6. 政策树
7. 任务树
8. 议会（右侧栏，高亮 SideRail）
9. 法案表决（密室政治）— 高亮提交法案按钮
10. **突击新闻发布会（丑闻响应）**（v1.3 新增；介绍触发条件、5 轮提问、3 秒倒计时、4 个选项）
11. **深夜官邸密室游说棋盘**（v1.3 新增；介绍 5×5 棋盘、WASD 走位、E 谈判、3 种策略）
12. 议会质询卡牌系统
13. 民意：信件与照会（高亮 SideRail）
14. 内阁（高亮 SideRail）
15. 外交
16. 时间与暂停
17. 事件决策
18. **百科全书（弹窗）**（v1.3 更新；强调百科现在是弹窗形式，在右上角菜单中打开，不再是独立页面）
19. **法律议事**（v1.4 新增；高亮 `.doc-card`；介绍 7 组法律 × 3 档、立法消耗政治资本与议会席位、2–6 个月审议期、跨系统传导）
20. **人物谱**（v1.4 新增；高亮 `.doc-card`；介绍 15 位 NPC 按 6 派系分组、性格态度、互动历史、施压事件）
21. **军事领域**（v1.4 新增；介绍军费/战备/兵力的资源代价与稳定/声望回报）
22. **经济领域**（v1.4 新增；介绍宏观经济引擎：GDP/失业率/通胀传导链，每项配颜色编码与趋势描述）
23. **社会领域**（v1.4 新增；高亮犯罪率/抗议频率/社会团结三项专属二级指标，以及人口支持分布）
24. **环境领域**（v1.4 新增；介绍生态四维度：空气/森林/水体/碳排放，以及季节性叙事卡）
25. **总理个人生活**（v1.4 新增；介绍家庭关系/腐败/压力三维，以及个人事件对全局的反向影响）
26. **跨系统联动事件**（v1.4 新增；介绍世界状态触发的加权事件，强调"决策非孤立"）
27. 教程完成

### 教程触发
- 进入 `playing` 阶段且 `tutorialCompleted === false` 时自动弹出
- 完成或跳过后写入 `settingsStore.tutorialCompleted`
- 可通过两个入口手动立即触发（`tutorialOpenSignal` 机制）：
  1. **右上角菜单 → 设置 → "▶ 立即重看新手教程"**：用户常用入口
  2. **右上角菜单 → 开发者选项（二次确认）→ "▶ 立即重看新手教程"**：演示/调试用
- 信号量非持久化（仅会话内有效），刷新页面后从 0 重新计数
- 不修改 `tutorialCompleted`，重看结束后会再次标记为已完成

---

## 23. 开发者控制台

### 入口变更（v1.2.2）
- **旧**：按 `~` / `` ` `` 键呼出（Game.tsx 全局键盘监听）
- **新**：右上角"菜单" → "🛠️ 开发者选项" → 二次确认弹窗 → 进入

**变更原因**：
1. `~` 键容易被误触，玩家可能在不知情情况下进入调试模式
2. 增加二次确认，避免误操作修改游戏状态
3. 与"百科、存档、设置、退出"等入口保持一致的导航体验

### 二次确认流程
1. 玩家在右上角菜单点击"🛠️ 开发者选项"
2. 弹出深色 zinc 主题确认对话框，明确说明：
   - 调整 turn 会改变游戏时间
   - 可能影响成就判定与结局触发
   - 建议仅在测试或排查问题时使用
3. 点击"确认进入" → 关闭菜单 + `setDevConsoleOpen(true)`
4. 点击"取消"或点击遮罩 → 取消操作

### DeveloperConsole 组件
状态由 `gameStore.devConsoleOpen` 管理（非持久化，会话级）：
- `setDevConsoleOpen(v: boolean)` 切换显示
- `loadGame` 时强制 `devConsoleOpen: false`，避免加载存档后残留状态

**功能**：
- 显示当前 `turn` / 日期 / `totalDays`
- 输入框 + 快捷按钮网格（-12 月、-1 月、+1 月、+12 月、跳到大选、跳到任期开始）
- 警告：会改变游戏时间，可能影响成就判定与存档兼容性
- **教程与辅助区**（v1.2.2 新增）：
  - "▶ 立即重看新手教程"按钮 → 调用 `triggerTutorial()` 立即开启教程
  - 适合在忘记机制或演示给他人时使用
  - 不影响成就判定

### ESC 键关闭
按 ESC 关闭控制台（保留原快捷关闭体验）

---

## 24. 待办提示系统

### 待办提示栏（PendingTodos.tsx）
v1.2 新增，v1.2.1 增强交互。挂在 SideRail 下方，显示当前未完成项目的醒目提示。

**显示规则**：
- 平时仅显示带颜色的小圆点 + 简短标签
- 鼠标悬停展开为图标 + 文字 + 详情
- **左键点击**：跳转到对应页面（侧面板或全屏页面）
- **右键点击**：忽略该待办（本回合不再显示，回合变化时自动清空忽略列表）
- **顶部收纳按钮**：点击▾收纳为一行汇总（显示三色圆点 + 总数），再点击展开
- 无待办时显示"✓ 就绪"指示

**视觉区分**：
- 强制/警告类待办：实心圆点 + 脉冲动画
- 鼓励性建议（encouraging）：空心圆圈，无脉冲，更安静

**颜色等级**：
| 等级 | 颜色 | 含义 | 圆点样式 |
|---|---|---|---|
| danger | 红 | 必须立即处理 | 实心+脉冲 |
| warning | 橙 | 应尽快处理 | 实心+脉冲 |
| info | 蓝 | 建议处理（鼓励性） | 空心圈 |

**收纳态**：
- 显示一行汇总：三色圆点叠加 + "N 待办" + ▸
- 点击展开恢复完整列表
- 标题提示分级计数（紧急/警告/建议）

**覆盖的 19 类待办**（v1.2.1 扩充）：

| 类别 | ID | 简短 | 触发条件 | 跳转 |
|---|---|---|---|---|
| 紧急 | `emergency` | 紧急事件 | currentEmergency 存在 | 仪表盘 |
| 紧急 | `countdown` | 倒计时 | currentCountdown 存在 | 仪表盘 |
| 紧急 | `low_approval` | 民意危急 | approval < 25 | 社会 |
| 紧急 | `low_stability` | 稳定危急 | stability < 25 | 社会 |
| 警告 | `event` | 待决策 | currentEvent 存在 | 仪表盘 |
| 警告 | `pmqs` | 质询应对 | activeCardEvent 存在 | 质询 |
| 警告 | `pmqs_due` | 质询到期 | shouldTriggerPmqs 为真 | 议会 |
| 警告 | `letters` | N 封信件 | pendingLetters > 0 | 信件 |
| 警告 | `notes` | N 件照会 | pendingNotes > 0 | 信件 |
| 警告 | `disloyal_cabinet` | N 部长不稳 | 内阁存在 loyalty < 45 | 内阁 |
| 警告 | `cabinet_chat` | N 条部长私信 | 内阁未读消息 > 0 | 内阁聊天 |
| 鼓励 | `no_initiative` | 未启动改革 | 执政满 3 月且无进行中改革 | 改革 |
| 鼓励 | `can_propose_bill` | 可提请法案 | 议会未解散且执政满 2 月 | 议会 |
| 鼓励 | `no_speech` | 未发表演说 | 执政满 4 月且距上次演说 ≥6 月 | 总理 |
| 鼓励 | `no_inspect` | 未视察地方 | 执政满 5 月且距上次视察 ≥8 月 | 总理 |
| 鼓励 | `no_diplomacy` | 未外交互动 | 执政满 4 月且所有国家距上次行动 ≥4 月 | 外交 |
| 鼓励 | `no_cabinet_review` | 未调整内阁 | 执政满 6 月且距上次调整 ≥6 月 | 内阁 |
| 鼓励 | `no_parliament_action` | 未与议会互动 | 执政满 5 月且距上次互动 ≥5 月 | 议会 |
| 鼓励 | `term_ending` | 任期将满 | turn ≥ 44 | 大选 |
| 鼓励 | `policy_check` | 可调整政策 | turn === 8（一次性） | 政策 |
| 鼓励 | `task_check` | 可查看任务 | turn === 10（一次性） | 任务 |

**交互细节**：
- 忽略列表按回合分桶：回合变化时自动清空，新回合重新评估所有待办
- 鼓励性待办与强制待办视觉区分：鼓励性用空心圈，避免视觉压力
- 收纳态保留分级颜色提示，玩家可快速判断紧急程度


---

## 25. 国家代入感与节令时序

### 节令时序叙事弹窗（v1.3 大幅增强互动感）

**组件**：`src/components/StoryBeatToast.tsx`
**位置**：屏幕左下角（`fixed bottom-6 left-6`），带 `data-story-beat-toast` 属性供教程高亮

**触发机制**：
- 由 `lastStoryDay` 追踪，每 N 天触发一次节令时序叙事
- 上下文敏感：根据当前局势（最弱指标、内阁忠诚度、季节等）选择匹配节拍
- 30 天后自动清除，或玩家互动后消失

**v1.3 互动增强**：
- 装饰性头部：显示节拍类别（节令时序/政治场景/国际视野）+ 关闭按钮
- 叙事正文：font-serif 排版，沉浸式内心独白
- **3 个互动选项**（取代原来的纯展示）：

| 选项 | 效果 | 适用场景 |
|---|---|---|
| 凝神片刻 | 无代价，小幅时间推进 | 默认选项，纯叙事体验 |
| 提笔记下 | 声望 +1，记录到回忆录 | 想积累长期声望时 |
| 召人商议 | 消耗 1 政治资本，获得幕僚见解（可能触发后续事件） | 政治资本充足时 |

**视觉呈现**：
- 卡片背景：渐变 `from-ink-900/95` + 金色边框
- 类别标识：节令时序 🍂 / 政治场景 🏛️ / 国际视野 🌐
- 入场动画：从左侧滑入（x: -50 → 0）
- 互动后：淡出退出

### 国家叙事节拍（nationalStory.ts）
v1.2 新增。20+ 条叙事节拍，按执政阶段（开局/中期/后期）与类别触发：
- **节令时序**：黎明街市、秋深议政等
- **政治场景**：人民来信、长桌之上、总统的书房等
- **国际视野**：异邦观察家等

**触发机制**：
- 由 `StoryBeatToast` 组件在特定条件下弹出
- 不影响游戏机制，纯叙事增强代入感
- 上下文敏感：根据当前局势选择匹配节拍

### 仪表盘每日简报（v1.2 重构）
仪表盘顶部"每日简报头"根据当前局势动态生成总理致辞：
- **局势危急**：最弱指标 <20 时红色警告
- **内阁不稳**：≥2 位部长忠诚度偏低时橙色提示
- **公文待批**：有未处理信件/照会时金色提示
- **民望尚可**：approval ≥60 时绿色鼓励
- **例行简报**：默认状态

### 国名徽章
仪表盘右上角显示国名首字徽章（SVG 边框 + 首字浮层），避免具体国旗的国家属性。

---

## 26. 难度系统

### 两档难度（v1.2 新增）
角色创建时选择：

| 难度 | 加成 | 惩罚 | 提示 |
|---|---|---|---|
| 普通难度 | 标准 | 标准 | 显示加成/惩罚数值 |
| 困难难度 | 缩放 | 放大 | 部分事件隐藏数值，需自行判断 |

### 困难难度特性
- 指标加成按系数缩放
- 指标惩罚按系数放大
- 特殊情境下（如道德困境）隐藏加成/惩罚提示
- 成就阈值更高，更难达成
- 适合有经验的玩家挑战

---

## 27. 玩法指南

### 开局流程
1. **主菜单** → 点击"新游戏"
2. **角色创建**：选择总理姓名、背景（3 选 1）、特质（5 项滑块）、**难度**（普通/困难）
3. **组阁谈判**：与潜在盟党谈判，组建执政联盟（需过半数席位）
4. **初始内阁组建**：从候补中任命 6 位内阁成员
5. **新手教程**：首次进入自动引导（可跳过）
6. **正式执政**：进入 `playing` 阶段，时间开始流动

### 核心循环
```
推进时间（按日） → 事件触发 → 玩家决策 → 指标变化
      ↓                                        ↓
  月末结算 ←←←←←←←←←←←←←←←←←←←←←←←←←←←←←←
      ↓
  改革结算 + 内阁加成 + 自然衰减 + 执政党耐心衰减 + 二级指标推导
      ↓
  检查结局条件（大选 / 提前下台 / 主动辞职）
```

### 策略建议
1. **指标平衡**：避免任一指标跌破危险线（特别是 approval <15、stability <10）。关注右侧待办提示栏的红色警告。
2. **改革规划**：优先推进与所选政党组阁要求契合的改革，维持 partyPatience。改革有前置链，长远规划。v1.3 改革树扩展至 L5 深度，深层改革收益更大但前置链更长。
3. **内阁管理**：定期检查忠诚度，及时改组低忠诚成员，必要时设立新部门扩充治理能力。回应部长私信避免背叛。
4. **议会互动**：confidence 低时主动质询或信任投票，席位不足时谨慎解散议会。善用**法案表决**密室政治拉票。
5. **资源管理**：politicalCapital 是稀缺资源（<30 时无法行动），重大改革与行动前需积累。
6. **事件响应**：7 天决策窗口内优先处理紧急事件，避免超时自动决策。倒计时事件必须当场决断。
7. **长期布局**：完成 5+ 改革解锁"改革先锋"历史标签，争取 S 级评级。
8. **信件照会**：及时回复选区信件与外交照会，拖久会扣指标。
9. **特质管理**（v1.3 新增）：
   - 关注健康值，<30 时行动次数受限且可能病休，适时通过事件回复提升
   - 果断 <30 会触发"优柔寡断惩罚"（正向效果 ×0.8），尽量保持 ≥30
   - 道德 <30 会增加丑闻触发概率与黑料卡代价，权衡短期利益与长期廉洁
   - 韧性高时可承受连续负面事件，低韧性时避免冒险决策
10. **新闻发布会应对**（v1.3 新增）：丑闻严重度 ≥60 时切入 minigame，根据当前特质选择策略——魅力高选"眼神锁定友好记者"，果断高选"怒斥记者"，劣势时选"避重就轻"减少损失。
11. **密室游说棋盘**（v1.3 新增）：步数有限（默认 8 步，健康<30 时 6 步），优先接近影响力大的代表；威胁策略虽成功率高但失败会触发新闻发布会，谨慎使用。
12. **节令时序互动**（v1.3 新增）：左下角弹窗出现时，政治资本充足可选"召人商议"获取见解，否则选"提笔记下"积累声望。
13. **教程与百科**：新手务必完成教程（v1.4 已扩充至 27 步）；游玩中忘记机制随时查百科（右上角菜单 → 百科，现为弹窗形式）。
14. **法律议事**（v1.4 新增）：法律是维多利亚 3 风格的长期制度安排，与改革（项目制）和政策树（方向性）均不同。优先立法"劳动法"与"治安法"以稳定社会基础，再视执政党倾向推进税制/医疗/教育法律。立法会消耗政治资本与议会席位，需提前积累。
15. **人物谱经营**（v1.4 新增）：定期查看 15 位 NPC 的态度变化，尤其关注军方与商界代表——冷落他们 ≥90 天会触发"登门施压"事件。施压事件选项多为让渡资源换支持，政治资本不足时谨慎应对。
16. **宏观经济传导**（v1.4 新增）：税率↑ → GDP 增长率↓ → 失业率↑ → 抗议频率↑ → 稳定↓；通胀↑ → 民意↓。经济页四项宏观指标仪表盘每月由 simulation.ts 自动结算，是预判 1–2 月后局势的领先指标。
17. **个人生活维护**（v1.4 新增）：压力值 >70 会触发健康下滑与决策失误；腐败度 >50 会触发丑闻风险；家庭关系 <30 会触发家庭危机事件影响执政。每月通过"家庭日"与"廉政自省"行动维稳。
18. **跨系统联动**（v1.4 新增）：注意事件描述中的世界状态引用——例如"失业率 >50"会加权触发工会施压事件，"邻国关系 <30"会加权触发边境摩擦事件。决策非孤立，每个选择都会通过 world state 影响后续事件权重。

### 结局目标
- **S 级**：六项指标均值 ≥85，解锁"青史留名的伟大总理"等历史定性
- **连任三届**：执政 12 年以上，解锁"三朝元老"成就与"长期执政"标签
- **专项成就**：经济奇迹（经济=100）、外交大师（外交=100）、满堂喝彩（六项≥80）、不倒翁（民意曾<20 后回升>50）
- **v1.4 新增目标**：法律完备（7 组法律全部 ≥2 档）、广结善缘（15 位 NPC 平均好感 ≥70）、清正廉洁（腐败度全程 <20）

---

## 27. 法律议事系统（v1.4 新增）

### 设计定位
v1.4 引入维多利亚 3 风格的法律议事系统，与原有改革、政策树形成"三轨制"：
- **改革**：项目制，有 duration，完成后产生一次性效果与解锁
- **政策树**：方向性，每类同时生效一项，切换有代价
- **法律**：制度性长期安排，立法后持续生效，改废需更高代价

### 7 组法律 × 3 档

| 法律组 | 主题 | L1 档 | L2 档 | L3 档 |
|---|---|---|---|---|
| 劳动法 | 劳资关系 | 基本工时保障 | 最低工资标准 | 完整劳动权益 |
| 税制 | 财政结构 | 单一税基 | 双税并行 | 复合税制 |
| 医疗 | 公共卫生 | 基础医保 | 全民医保 | 高水平保障 |
| 教育 | 国民教育 | 义务教育 | 高中普及 | 高等教育普及 |
| 治安 | 社会秩序 | 巡警制度 | 社区警务 | 智能治安网 |
| 选举 | 政治参与 | 限制选举 | 普选制 | 比例代表制 |
| 外交授权 | 外事权限 | 行政主导 | 议会审批 | 全民公决 |

### 立法流程
1. 玩家在法律页选择某法律组推进至下一档
2. 消耗 **政治资本**（基础 8，每档 +4）与 **议会席位**（基础 5%，每档 +3%）
3. 进入审议期：**2–6 个月**（视档位与争议性）
4. 审议期间有概率触发**辩论事件**（影响通过率）
5. 审议期结束 → 表决：基于当前议会席位与执政联盟支持度
6. 通过 → 档位 +1，立即生效；失败 → 政治资本返还 50%，声望 -3

### 跨系统传导
法律生效后持续影响多个系统：
- **劳动法 L2+**：就业率 +3、通胀 -2、商界 NPC 好感 -10
- **税制 L3**：财政盈余 +5、工业产出 -3、商界 NPC 好感 -15
- **医疗 L3**：民意 +5、国库 -8/月、社会团结 +3
- **教育 L3**：青年支持 +8、国库 -6/月、长期经济 +2
- **治安 L3**：犯罪率 -10、稳定 +4、公民自由争议事件
- **选举 L3**：议会席位波动加大、民意 +4
- **外交授权 L3**：外交 +5、议会审批延迟外交行动

### 数据结构
```typescript
// GameState.enactedLaws
enactedLaws: {
  labor: 2,      // 劳动法当前为第 2 档
  tax: 1,         // 税制为第 1 档
  healthcare: 3, // 医疗已至第 3 档
  // ...未立法的为 0
}
```

---

## 28. 人物谱与派系（v1.4 新增）

### NPC 扩展总览
v1.4 将原 5 位 NPC 扩展至 **15 位**，按 **6 大派系**分组，每位 NPC 有独立性格、初始态度与互动历史。

### 6 派系分组

| 派系 | 图标 | 色 | NPC | 影响场景 |
|---|---|---|---|---|
| 政界 | 🏛️ | 金 | 反对党领袖、中间派党魁、左翼党魁、右翼党魁、党内竞争对手 | 议会表决、组阁谈判 |
| 军方 | ⚔️ | 红 | 国防总长、海军司令 | 军费谈判、政变风险 |
| 商界 | 💼 | 绿 | 财阀代表、工业巨头 | 经济合作、施压事件 |
| 工会媒体 | 📣 | 蓝 | 工会主席、媒体大鳄 | 抗议触发、舆论引导 |
| 宗教 | 📿 | 紫 | 宗教领袖 | 社会团结、价值议题 |
| 外国政要 | 🌐 | 青 | 邻国大使、大国特使、国际组织代表 | 外交照会、制裁决议 |

### NPC 数据结构
```typescript
interface NPC {
  id: string
  name: string
  faction: string
  role: string          // 头衔
  personality: 'idealist' | 'pragmatist' | 'hardliner' | 'moderate' | 'opportunist'
  initialDisposition: number  // 0-100 初始好感
  interestExchange?: string   // 利益交换方向
  pressureEventId?: string    // 冷落 90 天后触发的施压事件
}
```

### 互动机制
- **好感度变化**：通过事件选项、改革方向、法律档位等动态影响
- **态度分级**：≥70 友好 / 40-69 中立 / 20-39 怨怼 / <20 敌对
- **施压事件**：好感度 <40 且 ≥90 天未互动时触发，要求让渡资源换支持
- **历史记录**：所有互动自动写入 `npcInteractionLog`，可在 NPC 卡片查看

### 页面入口
- 顶部主导航第 7 项"人物谱"，快捷键 **K**
- 点击 NPC 卡片展开详情面板：性格、当前态度、互动历史、相关事件
- 派系图标栏可一键筛选该派系下所有 NPC

---

## 29. 宏观经济模拟引擎（v1.4 新增）

### 设计目的
v1.4 引入宏观经济引擎，将原有的"经济指标 = 单一数字"升级为 **GDP / 失业率 / 通胀** 三维真实传导链，让税率、产业政策、改革完成度等真正联动到经济模型。

### 引擎位置
`src/engine/simulation.ts`，每月 `advanceMonth` 时由 `runMonthlySimulation` 调用。

### MacroEconomy 数据结构
```typescript
interface MacroEconomy {
  gdpTotal: number        // GDP 总量（基准 1000B）
  gdpGrowthRate: number   // 月增长率（%）
  unemploymentRate: number // 失业率（%）
  inflationIndex: number  // 通胀指数（0-100）
}
```

### 传导链
```
税率 → GDP 增长率 → 失业率 → 抗议频率 → 稳定
                       ↓
                    通胀 → 民意
```

**具体公式**（简化版）：
- `gdpGrowthRate = base(0.3) - taxRate×0.04 + industrialOutput×0.01 - reformProgressBonus`
- `unemploymentRate = clamp(8 - gdpGrowthRate×1.2 - employmentPolicyBonus, 2, 25)`
- `inflationIndex = clamp(50 + moneySupply - productivity + importedInflation, 0, 100)`
- `taxRevenue = gdpTotal × taxRate × 0.01`（直接影响国库）
- 战争期间额外消耗 GDP 与稳定

### 经济页仪表盘
经济页顶部展示四项宏观指标仪表盘：
- **GDP 总量**：色编码（绿 ≥1100B / 黄 900-1100B / 红 <900B）+ 趋势箭头
- **月增长率**：色编码（绿 >0.5% / 黄 -0.5~0.5% / 红 <-0.5%）
- **失业率**：色编码（绿 <5% / 黄 5-10% / 红 >10%）
- **通胀指数**：色编码（绿 <40 / 黄 40-60 / 红 >60）

### 与旧经济指标的关系
- 原 `metrics.economy`（0-100）保留作为"经济景气综合指数"
- 新增的宏观三维是 `metrics.economy` 的**驱动因子**：每月由 macroEconomy 推导出 economy 的目标值，再平滑过渡
- 旧存档加载时基于当前 `metrics.economy` 反推初始宏观值，无需手动迁移

---

## 30. 总理个人生活维度（v1.4 新增）

### 设计目的
v1.4 引入总理的"私人三维"——**家庭关系 / 腐败度 / 压力值**——让玩家不仅管理国家，还要管理总理本人。个人生活与全局指标相互影响，形成"以人治国"的代入感。

### PersonalLife 数据结构
```typescript
interface PersonalLife {
  familyRelation: number  // 0-100，越高越好
  corruption: number     // 0-100，越低越好
  stress: number          // 0-100，越低越好
}
```

### 三维影响

| 维度 | 危险区 | 触发后果 |
|---|---|---|
| 家庭关系 | <30 | 触发家庭危机事件：分居/离婚传闻，民意 -5、声望 -3 |
| 腐败度 | >50 | 触发丑闻调查事件：可能升级为新闻发布会 minigame |
| 压力值 | >70 | 触发健康下滑：健康特质 -10、当月行动次数 -1 |

### 反向影响（全局 → 个人）
- **民意 <30**：压力 +5/月
- **国库 <20**：压力 +3/月
- **改革失败**：压力 +8、家庭关系 -3
- **改革成功**：压力 -5、家庭关系 +2
- **道德特质 <30**：腐败度自然 +2/月
- **道德特质 >70**：腐败度自然 -1/月

### 维稳行动（总理日常行动扩展）
| 行动 | 冷却 | 效果 |
|---|---|---|
| 家庭日 | 30 天 | familyRelation +8，压力 -3 |
| 廉政自省 | 45 天 | corruption -6，声望 +1 |
| 度假休整 | 60 天 | stress -15，健康 +3，但本月行动次数 -1 |

### 个人事件池
v1.4 在事件池中新增 `personalEvents.ts`，包含 8+ 个个人事件：
- 子女入学丑闻（家庭关系 + 腐败交叉）
- 配偶公开信（家庭关系 + 民意交叉）
- 旧友求助（腐败 + 压力交叉）
- 健康警讯（压力 + 健康交叉）
- 等等

### 页面入口
- 总理档案页（PMProfilePage）新增"个人生活"面板
- 三项数值以仪表盘形式展示，配色彩编码与趋势描述
- 危险区数值会闪烁警告

---

## 31. 跨系统联动事件（v1.4 新增）

### 设计目的
v1.4 之前事件多为孤立的"触发 → 决策 → 结算"模式，决策与全局状态的相互影响弱。v1.4 引入**跨系统联动事件**机制，让事件根据当前 world state 动态加权触发，决策也通过 world state 反向影响后续事件权重。

### crossSystemEvents.ts
位于 `src/data/crossSystemEvents.ts`，包含 10+ 个跨系统事件，每个事件携带：
- `worldStateRefs`：引用的世界状态字段（如 `unemploymentRate`、`neighborRelations`）
- `triggerWeights`：基于世界状态值的动态权重函数
- `crossSystemEffects`：决策后对世界状态的反向修改

### 典型联动示例

**事件：工会施压加薪**
- 触发权重：`unemploymentRate > 8` 时权重 ×2，`protestFrequency > 50` 时权重 ×1.5
- 选项"同意加薪"：就业率 +2、通胀 +3、商界 NPC 好感 -10
- 选项"拒绝"：抗议频率 +10、民意 -5、工会主席好感 -15

**事件：边境摩擦升级**
- 触发权重：`neighborRelations < 40` 时权重 ×2.5，`military.spending < 30` 时权重 ×1.3
- 选项"外交斡旋"：邻国关系 +5、政治资本 -3
- 选项"军事对峙"：稳定 -3、邻国关系 -10、军方 NPC 好感 +5

**事件：商界联合施压减税**
- 触发权重：`taxRate > 35` 时权重 ×2，`industrialOutput < 50` 时权重 ×1.5
- 选项"让步减税"：国库 -8、商界 NPC 好感 +15、通胀 +2
- 选项"坚守"：商界 NPC 好感 -20、可能触发撤资事件链

### 触发检查
- 在 `advanceDay` 中每 **45 天**检查一次跨系统事件
- 由 `lastCrossSystemCheckDay` 追踪
- 同时最多 1 个跨系统事件入队，避免与普通事件冲突
- 跨系统事件享有**优先入队权**（超过 3 个 pending 时仍可加入）

### world state 引用
跨系统事件通过 `crossSystemFlags` 标志位防止重复触发：
- 每个事件有 `flagId`（如 `union_pressure_triggered`）
- 触发后 `crossSystemFlags[flagId] = true`
- 部分事件有 cooldown（通过 `eventCooldown` 机制），部分为一次性

---

## 32. 页面改版说明（v1.4 新增）

### SocietyPage 改版
v1.4 移除原通用模板（与 EconomyPage 同构的领域行动卡片网格），改为**社会专属**布局：
- **顶部社会指标仪表盘**：犯罪率 / 抗议频率 / 社会团结 三项专属二级指标，配色彩编码与说明
- **人口支持分布**：城市 / 农村 / 青年支持率横向条形图，直观展示民意构成
- **社会叙事卡片区**：根据当前最弱社会维度动态生成叙事（如犯罪率高时显示"治安警讯"卡片）
- **领域行动入口**：保留原社会领域行动，但移至底部折叠区，避免占主视觉

### EnvironmentPage 改版
v1.4 移除原通用模板，改为**环境专属**布局：
- **生态分数总览**：基于声望、社会团结、犯罪率综合推导的 0-100 生态分
- **生态四维度**：空气质量 / 森林覆盖 / 水体质量 / 碳排放强度，每项有专属图标与说明
- **污染源追踪**：列出当前主要污染源（基于工业产出、抗议频率等推导）
- **季节性叙事卡**：根据当前季节（春夏秋冬）生成对应环境叙事，如春季显示"复苏与新生"

### PMProfilePage 美化
v1.4 重新排版总理档案页：
- **顶部总理徽章 + 核心信息**：SVG 人像 + 姓名 + 任期 + 党派
- **左侧性格特质面板**：5 项特质滑块，每项配加成/惩罚说明
- **右侧执政资源面板**：政治资本 / 党内威望 / 辩论技巧 / 风险指数
- **底部个人生活面板**（v1.4 新增）：家庭关系 / 腐败度 / 压力值三维仪表盘
- **背景与执政履历**：背景故事 + 已完成改革列表
- 整体采用 12 栅格 + motion 动画，视觉层次清晰

### LawsPage（v1.4 新建）
v1.4 全新页面，展示法律议事系统：
- **7 个法律组卡片**：每个卡片显示当前档位（0/1/2/3）与下一档效果预览
- **立法进度条**：审议中法律显示剩余月数
- **跨系统影响提示**：每档法律下方列出对其他系统的影响
- **立法按钮**：满足条件（政治资本、议会席位）时可点击推进
- 卡片采用金边羊皮纸视觉风格，呼应"法律庄严感"

### NpcsPage（v1.4 新建）
v1.4 全新页面，展示 15 位 NPC：
- **顶部派系图标栏**：6 派系图标 + NPC 数量徽章，点击筛选
- **NPC 卡片网格**：每位 NPC 显示头像、姓名、头衔、性格标签、当前态度（色编码）
- **点击展开详情面板**：性格描述、初始态度、当前好感度、互动历史时间线
- **施压警告**：好感度 <40 或 ≥90 天未互动的 NPC 显示红色警告边框
- 卡片采用派系主题色边框，便于一眼识别归属

---

## 33. 历史曲线系统（v1.5 新增）

### 设计动机
v1.4 之前所有指标仅显示当前瞬时值，玩家无法感知"正在变好还是变坏"。v1.5 新增历史曲线面板，让玩家通过月度折线图看到趋势，提前预判危机或确认改革成效。

### 数据结构
- `GameState.metricHistory`：数组，每月由 `nextMonth` 自动推送一条记录
- 每条记录包含：`turn`（回合数）+ 6 项一级指标 + 3 项宏观经济指标
- 最多保留 60 条记录（覆盖完整 48 个月任期），超出后自动丢弃最旧记录
- 旧存档无此字段时初始化为空数组，下月起开始记录

### 组件实现
**`src/components/HistoryChart.tsx`**（单张折线图）
- SVG 折线图，含渐变填充区域 + 折线 + Y 轴极值标签 + X 轴起止月份
- 趋势徽章：绿色 ↑（向好）/ 红色 ↓（恶化）/ 灰色（平稳）
- 趋势判定：基于首末值变化量与阈值（首值 × 0.5%）
- 负向指标（失业率/通胀）的趋势方向会反转：下行=向好

**`src/components/HistoryChartsPanel.tsx`**（面板容器）
- 位于仪表盘底部，默认折叠，点击"展开 ▼"显示
- 上半部分：6 项一级指标 2–3 列网格布局
- 下半部分：3 项宏观经济（GDP 总量/失业率/通胀指数）3 列网格布局
- 顶部显示已记录月数与起止月份

### 集成位置
- `DashboardPage` 底部渲染 `<HistoryChartsPanel />`
- 数据写入：`gameStore.nextMonth` 中调用 `metricHistory.push(...)`
- 存档兼容：`loadGame` 中 `metricHistory: state.metricHistory ?? []` 兜底

---

## 34. 事件链深化（v1.5 新增）

### 设计动机
v1.4 的跨系统事件是独立触发的，世界缺乏连贯性。v1.5 将相关事件串联为多阶段剧情链，前一阶段的决策与状态会影响后续阶段是否触发，让世界更有连贯性。

### 数据结构
**`src/data/eventChainDefinitions.ts`** 定义 3 条链：

| 链 ID | 标题 | 阶段数 | 触发场景 |
|--------|------|--------|----------|
| `border_conflict_escalation` | 边境冲突升级链 | 4 | 边境摩擦 → 军事冲突 → 外交危机 → 制裁或战争 |
| `economic_crisis` | 经济危机链 | 4 | 衰退信号 → 挤兑 → 紧缩措施 → 复苏或崩溃 |
| `political_scandal` | 政治丑闻链 | 4 | 流言 → 媒体调查 → 议会调查 → 辞职或挺过 |

每阶段含：
- `stageId`：阶段唯一标识
- `eventId`：对应事件 ID
- `delayDays`：触发后延迟天数
- `condition`：触发条件函数（可选，未满足则跳过该阶段）

### 调度机制
- 玩家在某事件选项中通过 `chainId` 引用链 → 加入 `pendingChains`
- `eventEngine` 在 `advanceDay` 中检查 `pendingChains`，到达 `triggerDay` 且满足条件时触发下一阶段
- 前一阶段触发后会自动延后进入下一阶段判定
- 未满足条件的阶段会被跳过，链可能提前终止
- 链状态可通过事件详情的 `chainInfo` 字段查看

### 存档兼容
- `loadGame` 中 `pendingChains: state.pendingChains ?? []` 兜底
- 旧存档加载后不会丢失进行中的链，下月起继续调度

---

## 35. NPC 主动 AI（v1.5 新增）

### 设计动机
v1.4 的 NPC 是被动等待事件触发的，世界显得空洞。v1.5 让 NPC 真正"活"起来：根据自身状态主动发起来电、拜访或公开声明，不再被动等待。

### 数据结构
**`src/data/npcProactiveActions.ts`** 定义 NPC 主动行动：
```typescript
interface NPCProactiveAction {
  npcId: string
  type: 'call' | 'visit' | 'statement'  // 来电/拜访/公开声明
  title: string
  description: string
  trigger: (state: GameState) => boolean  // 触发条件
  options: {
    id: string
    label: string
    effects: Partial<Metrics>
    npcDispositionDelta?: number  // NPC 好感度变化
    newsTitle: string
    newsSummary: string
  }[]
}
```

### 触发机制
- 每 60 天检查一次（由 `lastNpcProactiveCheckDay` 追踪）
- 由 `eventEngine.checkNPCProactiveActions` 在 `advanceDay` 开头调用
- 符合状态的 NPC 会主动行动，包装成 `PendingEvent` 加入队列
- 14 天内必须决策，否则按默认选项处理
- 同一 NPC 在 240 天冷却期内不会再次主动行动（由 `eventCooldowns` 追踪）
- 当 `pendingEvents` 已有 3 个待决策事件时，检查会被跳过，避免队列过载

### 触发条件示例
- **国防部长周振国**：军力 < 40 且国库 > 30 且好感 > 30 → 致电要求增加军费
- **首富钱万通**：腐败 > 30 → "二次登门"提议利益交换
- **工会主席孙铁柱**：抗议 > 50 → 登门施压
- **宗教领袖**：社会团结 < 35 → 公开发表声明

### 决策影响
每次主动行动有 3 个选项（如批准/协商/拒绝），决策会影响：
- NPC 好感度（`npcDispositionDelta`）
- 国家指标（`effects`）
- 新闻报道（自动生成）

### 存档兼容
- `loadGame` 中 `lastNpcProactiveCheckDay: state.lastNpcProactiveCheckDay ?? 0` 兜底
- 旧存档加载后从 0 开始计时，60 天后开始首次检查

---

## 36. 战争指挥面板（v1.5 新增）

### 设计动机
v1.4 的战争是数值结算，缺乏战术维度。v1.5 为战争加入作战指挥界面（WarCommandPanel），让玩家有真实的前线部署与将领调度体验。

### 触发条件
- 仅在 `activeWar` 存在且未结束时于军事页显示
- 由 `MilitaryPage` 渲染 `<WarCommandPanel />`

### 数据结构
```typescript
interface WarCommandState {
  deployments: FrontDeployment[]      // 前线部署
  availableGenerals: WarCommandGeneral[] // 可调遣将领
  warExhaustion: number               // 战争疲劳度 0-100
  supplyLines: number                 // 补给线完整度 0-100
}

interface FrontDeployment {
  sector: string                      // 战区名
  ourStrength: number                 // 我军兵力
  enemyStrength: number               // 敌军兵力
  status: 'advancing' | 'retreating' | 'stalemate' | 'holding'
  assignedGeneralId?: string          // 当前指挥将领 ID
}

interface WarCommandGeneral {
  id: string
  name: string
  skill: number                       // 将领技能值
  assignedSector?: string            // 已指派的战区
}
```

### 玩家可执行操作
1. **将领调度**：点击可调遣将领按钮 → 指派到未指派战区；点击 × 取消指派
2. **将领加成**：将领技能按 0.25 倍率加成我军战力（`ourStrength += skill × 0.25`）
3. **紧急增援**：消耗国库（≥6）+ 政治资本（≥3）→ 二次确认 → 补充兵力

### 全局指标
- **战争疲劳度**（0–100，越高越糟）：随战争持续上升，过高会拖累民意
- **补给线完整度**（0–100，越高越好）：过低会削弱战区战力，需通过外交或军事行动维护

### Store Actions
- `assignGeneralToSector(sectorId, generalId)`：指派将领
- `unassignGeneralFromSector(sectorId)`：取消指派
- `reinforceSector(sectorId)`：紧急增援

### 存档兼容
- `loadGame` 中 `warCommand: state.warCommand ?? null` 兜底
- 旧存档加载后 `warCommand` 为 null，战争指挥面板不显示，下次开战时自动初始化

---

## 37. 页面布局调整与 Bug 修复（v1.5）

### 导航结构调整
v1.5 将"总理档案"与"人物谱"从主导航移至右侧 SideRail 栏（与议会/民意/内阁同模式）：
- **主导航**（顶部横向）：仪表盘/任务树/政策树/改革/法律/外交/军事/经济/社会/环境/新闻
- **右侧栏总理组**：总理档案（F）/ 人物谱（K）
- 点击后以右侧滑出面板形式展示，不再占用全屏

### 领域页面全屏滚动
v1.5 修复了经济部、军事部、环境部、社会部页面滚动区域过小的问题：
- 移除 `DomainPageLayout` 内部的嵌套滚动容器
- 改为全屏可滚动布局，内容自然延展，玩家可流畅浏览所有信息

### Bug 修复
1. **"不倒翁"成就判定**：原逻辑未正确追踪民意低谷与回升。v1.5 新增 `lowestApproval` 与 `approvalRecoveryAchieved` 字段，确保民意真正跌至 20 以下后回升至 50 才解锁。
2. **宣战显示"本局已使用"**：原冷却逻辑未检查 `lastActionTurn > 0`，导致首次宣战即被误判为已使用。v1.5 修复判定条件。
3. **外交缺少"破坏关系"选项**：v1.5 新增 `damage_relations` 外交行动，允许主动恶化与某国关系，为外交博弈提供更多选择。

### 存档系统迁移
v1.5 新增字段的存档兼容性：
- `metricHistory: state.metricHistory ?? []`（历史曲线数据）
- `warCommand: state.warCommand ?? null`（战争指挥状态）
- `pendingChains: state.pendingChains ?? []`（多阶段事件链）
- `lastNpcProactiveCheckDay: state.lastNpcProactiveCheckDay ?? 0`（NPC 主动行动检查日）
- `lowestApproval: state.lowestApproval ?? 100`（不倒翁成就追踪）
- `approvalRecoveryAchieved: state.approvalRecoveryAchieved ?? false`（不倒翁成就追踪）

旧存档可平滑加载，v1.5 新系统下月起自动开始记录数据。

### 教程扩展
v1.5 教程从 26 步扩展至 30 步，新增 4 个步骤：
1. 历史曲线（仪表盘底部折叠面板介绍）
2. 事件链深化（3 条多阶段剧情链说明）
3. NPC 主动 AI（来电/拜访/公开声明机制）
4. 战争指挥面板（前线部署/将领调度/紧急增援）

教程的 `SIDE_RAIL_PAGES` 集合已同步更新，包含 `pm_profile` 与 `npcs`，确保教程跳转时正确打开右侧面板。

### 百科扩展
v1.5 新增 4 个百科条目：
- `mechanic_historyCharts`：历史曲线系统
- `mechanic_eventChainDeepen`：事件链深化
- `mechanic_npcProactive`：NPC 主动 AI
- `mechanic_warCommand`：战争指挥面板

---

## 附录：通用化设定

为避免具体国家属性，本游戏采用通用化设定：
- **国名**：由玩家在角色创建时自定义，或使用默认"共和国"
- **总统**：随机生成姓名，60% 概率与总理同党
- **邻国**：通用化称谓，无具体国名
- **事件文案**：已移除"长江"等具体地名，改用"流域"、"南部多省"等通用表述
- **国名徽章**：仪表盘显示国名首字 SVG 徽章

---

*本文档由 prideplayer 维护，最后更新于 2026-07-28，版本 1.5.0（历史曲线 + 事件链深化 + NPC 主动 AI + 战争指挥面板 + 页面布局调整 + Bug 修复 + 教程/百科/存档系统扩展）。*
